name: PR Analysis

on:
  pull_request:
    branches:
      - main

jobs:
  analysis:
    name: Analyse PR
    uses: ./.github/workflows/pr-analysis-maven.yml
    with:
      use-codeartifact: true
    secrets:
      sonar-token: ${{ secrets.SONAR_TOKEN }}
